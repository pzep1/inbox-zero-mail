# Inbox Zero 0.1.3

- Fixed direct-download updates for sandboxed macOS builds.
- Added release validation so Sparkle update support cannot ship without the required sandbox installer configuration.
- Reduced Gmail sync load and hardened rendered email HTML.
- Kept signed image-proxy URLs aligned with the web app contract.

If you installed 0.1.0, 0.1.1, or 0.1.2, download and replace the app manually once. Those builds cannot launch Sparkle's sandbox installer.
